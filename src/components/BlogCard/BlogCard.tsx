import { IBlog } from "../../types";

import "./BlogCard.scss";

const BlogCard = ({
  title,
  author,
  categories,
  description,
  id,
  image,
  publish_date,
}: IBlog) => {
  return <div>BlogCard</div>;
};

export default BlogCard;
